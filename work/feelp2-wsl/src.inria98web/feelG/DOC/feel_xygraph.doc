ＸＹグラフ描画ルーチン

SUBROUTINE FEEL_XYGRAPH(NLINE,NODES,PX,PY,SX,SY,PPX,PPY,
     $PXMIN,PXMAX,PYMIN,PYMAX,NXDIV,NYDIV,NAXIS,NYAS,NSTEP,IRGF,IWD)

◎各引き数の意味                                       配列の大きさ
 
      NLINE   :グラフの本数
      NODES   :グラフ１本あたりの点の数
      PX      :各グラフのＸの配列                      NODES × NLINE
      PY      :各グラフのＹの配列                      NODES × NLINE
      SX      :各グラフのＸの作業領域                  NODES × NLINE
      SY      :各グラフのＹの作業領域                  NODES × NLINE
      PPX     :各グラフのＸの作業領域                  NLINE
      PPY     :各グラフのＹの作業領域                  NLINE
      PXMIN   :Ｘ軸の最小値の配列(IRGF=1の時のみ参照)  NLINE
      PXMAX   :Ｘ軸の最大値の配列(IRGF=1の時のみ参照)  NLINE
      PYMIN   :Ｙ軸の最小値の配列(IRGF=1の時のみ参照)  NLINE
      PYMAX   :Ｙ軸の最大値の配列(IRGF=1の時のみ参照)  NLINE
      NXDIV   :Ｘ軸の分割数の配列(IRGF=1の時のみ参照)  NLINE
      NYDIV   :Ｙ軸の分割数の配列(IRGF=1の時のみ参照)  NLINE
      NAXIS   :Ｙ軸フラグの配列 0:書かない 1:Y1 2:Y2   NLINE
      NYAS    :Ｙ軸レンジ統一フラグ 0:しない 1:する    
      NSTEP   :グラフのステップ数
      IRGF    :レンジフラグ ０：Auto １：マニュアル
      IWD     :ウィンドウＩＤ番号
 -----------------------------------------------------------------------
      注意事項１   :１０ライン以上は表示できません
      注意事項２   :IRGF について
                    (0)PXMIN,PXMAX,PYMIN,PYMAX,NXDIV,NYDIV
                       いずれも定義する必要はありません
                    (1)PXMIN,PXMAX,PYMIN,PYMAX,NXDIV,NYDIV
                       を定義してください
 -----------------------------------------------------------------------

