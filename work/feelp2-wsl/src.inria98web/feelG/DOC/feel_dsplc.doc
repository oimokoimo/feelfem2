変位図描画ルーチン

SUBROUTINE FEEL_DSPLC(NP,NE,NN,NEN,PX,PY,QX,QY,U,V,FT,SC,
     $IC,ISL,ICT,IDV,ICS,ITP,ICM,SMIN,SMAX,ICB,NCBX,NCBY,ID,IWD)

◎各引き数の意味                                       配列の大きさ
 
      NP      :節点数
      NE      :要素数
      NN      :各要素の節点数
      NEN     :要素、節点番号が格納されている配列      NN × NE 
      PX      :節点のＸ座標値                          NP
      PY      :節点のＹ座標値                          NP
      QX      :作業領域(変位後のＸ座標値)              NP
      QY      :作業領域(変位後のＹ座標値)	       NP
      U       :Ｘ座標値の変位量                        NP
      V       :Ｙ座標値の変位量                        NP
      SC      :各座標点のスカラ値                      NP
      FT      :倍率
      IC(1)   :元の図のカラーＩＤ番号
      IC(2)   :変位後の図のカラーＩＤ番号
      ISL     :変位後の図の背景を
               0:塗りつぶす、1:塗りつぶさない
      ICT     :変位後の図のコンタを 0:書かない 1:書く
      IDV     :コンタのしきい値数
      ICS     :コンタのしきい値フラグ 
               0:Auto 1：マニュアル 2:Full Auto
      ITP     :コンタの表示タイプ 0:線画 1:塗りつぶし
      ICM     :変位後のメッシュ 0:書かない 1:書く
      SMIN    :コンタのしきい値の最小値
      SMAX    :コンタのしきい値の最大値
      ICB     :カラーバーの有無 0:なし 1:あり
      NCBX    :カラーバーの位置Ｘ
      NCBY    :カラーバーの位置Ｙ
      ID      :要素形状(３角形 = 3  ４角形 = 4)
      IWD     :ウィンドウＩＤ番号
 -----------------------------------------------------------------------
      注意事項１   :各要素の節点数の最大は１００とします。
      注意事項２   :ICS について
                    (0)IDV を定義しなければなりません
                    (1)IDV,SMIN,SMAX を定義しなければなりません
                    (2)IDV,SMIN,SMAX いずれも定義する必要はありません
 -----------------------------------------------------------------------
