solve文でのデータstructure
----------------------------------------------------------------------

近似要素の情報

節点数
節点なし自由度数(自由度は全体)

各局所節点の未知変数リスト

------------------------------------------------------------------------
FORTRANプログラムでの変数

規約   定数的な変数は'n'で始まる

NUMNP  総節点数
NEG    現在の問題に対する要素グループの数

【要素データから読み込まれるもの】
NUME   要素数
NED    要素内自由度
NEP    要素内節点数
IELEM(NEP,NUME) 局所節点番号←→全体節点番号対応配列
IEF(NEP)        要素の各局所節点毎の自由度

【連立一次方程式の為の情報変数】
NEQ   方程式の数(ディリクレ境界条件が課されている節点を含む）
NSK   上三角行列の非対角成分の一次元配列としての大きさ

［solve文に対応するもの］
IPD(NUMNP)      累積節点自由度(最初は各節点自由度の計算に用いる)
IED(NUME)       各要素の節点less自由度の方程式番号情報配列（一時変数）
［各要素に対してあるもの］
IWISE(NUMNP)    要素自由度(節点less自由度)がある場合のワーク配列

一般に

AU(*)           係数(上三角部分)
AD(*)           係数(対角項)
AL(*)           係数(下三角部分)
KLD(*)          スカイラインの為の情報配列

B(*)            右辺ベクトル(荷重)、解


